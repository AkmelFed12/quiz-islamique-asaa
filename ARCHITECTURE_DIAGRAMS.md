# 📊 Architecture Diagrams

## System Architecture

### High-Level Overview

```
┌────────────────────────────────────────────────────────────────┐
│                      Islamic Quiz App                          │
│                       React (Vite)                             │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │              Services Layer                              │ │
│  │                                                          │ │
│  │  ┌──────────────────┐  ┌──────────────────┐           │ │
│  │  │ storageService   │  │  geminiService   │           │ │
│  │  │ (All CRUD ops)   │  │  (AI questions)  │           │ │
│  │  └────────┬─────────┘  └──────────────────┘           │ │
│  │           │                                             │ │
│  │           └──────────────┬───────────────────┐         │ │
│  │                          │                   │         │ │
│  └──────────────────────────┼───────────────────┼─────────┘ │
│                             │                   │           │
│         ┌───────────────────┘                   │           │
│         │                                       │           │
│    ┌────▼──────────────────────────────────────┴────┐     │
│    │         Connection Layer                       │     │
│    │                                                │     │
│    │  ┌─────────────────────────────────────────┐  │     │
│    │  │         dbPool.ts                       │  │     │
│    │  │  • Connection management                │  │     │
│    │  │  • Retry logic (3x)                     │  │     │
│    │  │  • Connection pooling (max 10)          │  │     │
│    │  │  • Error handling                       │  │     │
│    │  └─────────────────┬───────────────────────┘  │     │
│    │                    │                           │     │
│    │  ┌─────────────────▼───────────────────────┐  │     │
│    │  │      dbSchema.ts                        │  │     │
│    │  │  • Schema initialization                │  │     │
│    │  │  • Table creation                       │  │     │
│    │  │  • Index management                     │  │     │
│    │  └─────────────────────────────────────────┘  │     │
│    │                                                │     │
│    │  ┌─────────────────────────────────────────┐  │     │
│    │  │      dbMigration.ts                     │  │     │
│    │  │  • LocalStorage → PostgreSQL migration  │  │     │
│    │  │  • Data verification                    │  │     │
│    │  │  • Statistics                           │  │     │
│    │  └─────────────────────────────────────────┘  │     │
│    └────────────────────────────────────────────────┘     │
│                                                            │
└────────────────────────────────────────────────────────────┘
         │                                    │
         │                                    │
    ┌────▼──────────────┐          ┌─────────▼──────────┐
    │   PostgreSQL      │          │   LocalStorage     │
    │  (Neon.tech)      │          │   (Browser)        │
    │                  │          │                    │
    │ • 5 Tables       │          │ • JSON blobs       │
    │ • Indexes        │          │ • Key-value pairs  │
    │ • 10 Connections │          │ • Always available │
    │ • SSL Encrypted  │          │ • No setup needed  │
    └─────────────────┘          └────────────────────┘
```

---

## Data Flow Diagram

### User Sign-Up Flow

```
User clicks "Sign Up"
        │
        ▼
┌──────────────────────┐
│  Auth Component      │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  saveUser(newUser)                  │
│  services/storageService.ts         │
└──────┬───────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  getPool()                           │
│  returns pool or null                │
└──┬──────────────────────────────────┘
   │
   ├─ YES ──────────┐
   │                │
   │ NO             ▼
   │           PostgreSQL
   │           Connection
   │           │
   │           ▼
   │    INSERT user
   │           │
   │           ▼
   │    ✅ User saved
   │
   └──────────┐
              │
              ▼
    ┌─────────────────────┐
    │  localStorage       │
    │  asaa_db_users      │
    │  (fallback)         │
    │                     │
    │  ✅ User saved      │
    └─────────────────────┘
```

### Quiz Result Flow

```
User completes quiz
        │
        ▼
┌──────────────────────┐
│  Quiz Component      │
│  calculateScore()    │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  saveResult(quizResult)          │
│  services/storageService.ts      │
└──────┬──────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  Save to Database / LocalStorage │
│  (via getPool())                 │
└──────┬──────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  checkBadges()                   │
│  Calculate earned badges         │
└──────┬──────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  awardBadge() for each new badge │
│  Save to user_badges table       │
└──────┬──────────────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  Update user.lastPlayedDate      │
└──────┬──────────────────────────┘
       │
       ▼
✅ Quiz Result Saved + Badges Awarded
```

### Data Query Flow

```
Component needs data
        │
        ▼
┌──────────────────────────┐
│  getResults()            │
│  getUsers()              │
│  getQuestionsBank()      │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────────────────────┐
│  getPool()                       │
└──┬───────────────────────────────┘
   │
   ├─ Pool exists? ──────────┐
   │                         │
   YES                       NO
   │                         │
   ▼                         ▼
PostgreSQL              localStorage
   │                         │
   ▼                         ▼
SELECT query           JSON.parse()
   │                         │
   ▼                         ▼
Parse Results          Get Array
   │                         │
   └────────┬────────────────┘
            │
            ▼
        Return Data
            │
            ▼
        Update UI
```

---

## Connection Lifecycle

```
┌─────────────────────────────────────────────────────────────────┐
│                   App Startup                                  │
│                     (App.tsx)                                  │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       ▼
           ┌─────────────────────────┐
           │  initializePool()       │
           │  dbPool.ts              │
           └────────┬────────────────┘
                    │
        ┌───────────┴────────────┐
        │                        │
        ▼                        ▼
  Check DATABASE_URL      DATABASE_URL
  in environment          not set
        │                        │
        ▼                        ▼
  Try connect            ⚠️ Pool = null
        │                │
        │        ┌───────┘
        │        │
    ┌───┴────────┴──────┐
    │                   │
SUCCESS             FAIL
    │                   │
    ▼                   ▼
✅ Connected         ⚠️ Retry (max 3x)
    │                   │
    │                   ├─ Retry 1 (wait 2s)
    │                   │
    │                   ├─ Retry 2 (wait 2s)
    │                   │
    │                   ├─ Retry 3 (wait 2s)
    │                   │
    │                   ▼
    │                   ❌ Give up
    │                   Pool = null
    │                   │
    └───────────┬───────┘
                │
                ▼
        ┌─────────────────────┐
        │  initDB()           │
        │  storageService.ts  │
        └────────┬────────────┘
                 │
        ┌────────┴──────────┐
        │                   │
        ▼                   ▼
  Initialize Schema    Initialize LocalStorage
  (if pool exists)     (always)
        │                   │
        └───────────┬───────┘
                    │
                    ▼
            📍 Ready to use!
```

---

## Fallback Decision Tree

```
                    Operation Called
                         │
                         ▼
                    getPool() != null?
                    │           │
                  YES           NO
                    │           │
                    ▼           ▼
            Try PostgreSQL  Use LocalStorage
                    │           │
                    ▼           ▼
            Success?        ✅ Saved to
                │          localStorage
              / │ \
           YES  │  NO
            /   │   \
           ▼    │    ▼
        ✅Data │  ⚠️ Query
        saved  │  Failed
               │
               ▼
         Fallback to
         LocalStorage
               │
               ▼
         ✅ Saved to
         localStorage
```

---

## Database Schema Relationship Diagram

```
users
┌──────────────────────────────┐
│ username (PK)  ◀─────────────┼──── Primary Key
│ role                         │
│ last_played_date             │
│ created_at                   │
│ updated_at                   │
└──────────────────────────────┘
     ▲              ▲
     │              │
 ┌───┘              └────────┐
 │                           │
 │ (1:N)                 (1:N)
 │                           │
 ▼                           ▼
results               user_badges
┌──────────────────┐  ┌──────────────────┐
│ id (PK)          │  │ username (FK)    │
│ username (FK)    │  │ badge_id         │
│ score            │  │ date_earned      │
│ total_questions  │  │ created_at       │
│ date             │  │                  │
│ created_at       │  │ PK: (username,   │
│                  │  │      badge_id)   │
│ INDEX:           │  └──────────────────┘
│ idx_results_     │
│ username         │
└──────────────────┘

questions
┌──────────────────────────┐
│ id (PK)                  │
│ question_text            │
│ options (JSONB)          │
│ correct_index            │
│ explanation              │
│ difficulty               │
│ source                   │
│ created_at               │
│ updated_at               │
│                          │
│ INDEX:                   │
│ idx_questions_difficulty │
└──────────────────────────┘

global_state
┌──────────────────┐
│ key (PK)         │
│ value (JSONB)    │
│ updated_at       │
└──────────────────┘
```

---

## Error Handling Flow

```
       Operation Called
              │
              ▼
       Try Execute Query
              │
         ┌────┴────┐
         │         │
       OK      FAIL
         │         │
         ▼         ▼
    Return Data   Check if
                  pool exists
                   │      │
                Pool   No Pool
                 │         │
                 ▼         ▼
            Log Error   Try LocalStorage
            Fallback  
            to Local
            Storage
                 │         │
                 └────┬────┘
                      │
                      ▼
              Return Data/Error
```

---

## Performance Characteristics

```
PostgreSQL (Connected)
├─ Connection time: ~200ms (first time)
├─ Query time: 50-200ms
├─ Best for: Production, multiple users
└─ Cost: Free tier available

LocalStorage (Fallback)
├─ Access time: < 5ms
├─ Best for: Development, single user
├─ Limitation: ~5-10MB storage
└─ Cost: Free (browser storage)

Choice: Automatic based on availability
Result: Optimal performance either way
```

---

## Deployment Topology

```
┌──────────────────────────────────────────┐
│         Cloud Hosting Platform           │
│      (Vercel / Netlify / AWS)            │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │     React App (Static + JS)        │ │
│  │     - Served globally via CDN      │ │
│  │     - Environment variables loaded │ │
│  └───────────────┬────────────────────┘ │
│                  │                      │
│  ┌───────────────▼────────────────────┐ │
│  │  .env vars (DATABASE_URL, etc)    │ │
│  └───────────────┬────────────────────┘ │
└──────────────────┼─────────────────────┘
                   │
        ┌──────────┴──────────┐
        │                    │
        ▼                    ▼
   PostgreSQL           Browser
   (Neon.tech)         (Client)
   Remote DB           LocalStorage
        │                    │
        └────────┬───────────┘
                 │
        Data persisted
        either way
```

---

**These diagrams show how your database system works architecturally. For implementation details, see the service files and code comments.**
